# 2026-04-13

- Usuário: Junior (Cuiabá-MT, fuso America/Cuiaba), proprietário da **JR Auto Parts**.
- Pedido: que eu atue como “braço direito”, ajudando a cuidar do que for preciso da empresa.
- Preferência operacional: pedir autorização somente quando for excluir algo.

## Resumo recebido da operação JR Auto Parts

- Operação híbrida: autopeças + oficina + relacionamento automatizado (WhatsApp e portal do cliente).
- Entidades centrais do sistema: Client, Vehicle, Product, Service, ServiceOrder, SoItem, SoStatusLog, PreventiveMaintenance, WhatsappMessage.
- Catálogo de produtos com preço/estoque/status; bot consulta catálogo para atendimento.
- Serviços seed: troca de óleo/filtro, alinhamento/balanceamento, revisão, correia dentada, diagnóstico eletrônico, pastilhas de freio, higienização A/C, amortecedores.
- OS como centro: km obrigatório na abertura; logs de status (QUOTE, APPROVED, STARTED, IN_PROGRESS, WAITING_PART, THIRD_PARTY, RETIFICA, FINISHING, DONE, DELIVERED).
- Manutenção preventiva por km/tempo (o que vencer primeiro) com estados (EM_DIA, ATENCAO_PROXIMA, VENCIDO, VENCIDO_CRITICO).
- Atendimento principal via WhatsApp, bot com sessão por telefone, contexto de cliente/veículos/OS, escala para humano com marcador [HANDOFF].
- Interfaces: gestão interna (dashboard, OS, clientes, veículos, produtos, serviços, manutenção, mensagens) e portal do cliente (veículos, alertas, histórico/status de OS).
- Stack: React+Vite, Node+Express, Prisma, Postgres/Supabase, Render (backend), Vercel (frontend), Cloudinary (imagens), Resend (email), BotConversa (WhatsApp).
- Segurança: JWT/bcrypt, rate limits, lockout, senha forte, troca obrigatória no 1º uso.
- Módulo extra: CompanyAsset e DigitalAccount (ativos e contas/canais digitais).
- Escopo V1: OS com km e histórico, notificação WhatsApp, preventiva, portal, usuários/perfis, cadastros base e financeiro básico ligado à OS; fora: guincho com mapa, locação completa, e-commerce 3k itens e integração completa Olist ERP.

## OpenClaw (VPS Hostinger, Ubuntu 24.04)

- Migração do ambiente Docker gerenciado para execução no host.
- Gateway rodando como serviço systemd `openclaw-gateway.service` (user/group `openclaw`).
- Porta mantida: 47638; bind em 0.0.0.0; auth por token.
- Estado/config migrados de `/docker/openclaw-r4fv/data/.openclaw/` para `/srv/openclaw/home/.openclaw/` (ownership corrigido).
- Secrets centralizados em `/etc/openclaw/openclaw.env` com permissões restritas.
- Container antigo do OpenClaw ficou parado (evitar conflito de porta/plugins).
- Tentativa anterior `openclaw gateway install` falhou por depender de `systemctl --user` (sem user bus em servidor headless); solução foi serviço system-level.

## Segurança/boas práticas

- Junior compartilhou uma senha no chat; não registrar nem reutilizar aqui, orientar a não enviar credenciais em mensagens.

## OpenClaw Control UI

- Erro visto ao acessar via IP/HTTP: `control ui requires device identity (use HTTPS or localhost secure context)`.
- Para acesso remoto: usar HTTPS no domínio (`https://srv1555250.hstgr.cloud/...`) e WebSocket `wss://srv1555250.hstgr.cloud`, ou usar túnel SSH e acessar por `localhost`.

## Ollama (VPS)

- Ollama instalado e rodando em `http://127.0.0.1:11434`.
- Modelo local baixado para uso: `qwen3:8b` (CPU-only).
- Integração com OpenClaw habilitada via `OLLAMA_API_KEY` no environment do gateway (auto-discovery), e `openclaw models list` passou a mostrar `ollama/qwen3:8b` (alias: “Qwen3 8B (local)”).
- Limitação: nessa VPS (15GB RAM/4 vCPU, sem GPU), modelos ~32B (ex.: Qwen3 32B) não são viáveis na prática (muito lento/risco de OOM); recomendado VPS maior ou Ollama em outra máquina e apontar `baseUrl`.
